{% extends "second.php" %}
<section>
    <div class="container">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    {% for item in list %}
                        {{ item|raw }}
                    {%endfor%}
                </div>
            </div>
        </div>
    </div>
</section>