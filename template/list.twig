<section>
    <div class="container">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <ul>
                            {% for item in list %}
                                <li class="nav-item m-3">
                                    <a class="nav-link" href="/article/{{ item.id }} ">{{ item.title}}</a>
                                </li>

                            {% endfor %}
                    </ul>
                </div>
            </div>
        </div>
    </div>
</section>